{{!-- each item is going to display the user input in basically the same format as my models
so I should be generating something to the extent of workout.workout_name, workout.weight, 
workout.repetitions, workout.sets 
use partials to iterate over array and display my workouts use #each to iterate over db--}}